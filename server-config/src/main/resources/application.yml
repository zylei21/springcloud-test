server:
  port: 8080
spring:
  application:
    name: microservice-config-server
  cloud:
    config:
      server:
        git:
          # Git仓库地址
          uri: https://github.com/zylei21/springcloud-test.git
          # Git仓库账号
          username: zylei21
          # Git仓库密码
          password: Lei211314